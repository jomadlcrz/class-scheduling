/** Mobile drawer navigation — reserved for future use. Desktop uses the inline drawer in app-shell.tsx. */

export function MobileNav(_props: { open: boolean; onClose: () => void }) {
  return null;
}
